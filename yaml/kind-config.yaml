# Setup for a simple Kind multi node Kubernetes cluster
# One control plane node
# Three worker nodes
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker
